import { defaultTheme } from './global/theme/defaultTheme'

const theme = {
  ...defaultTheme,
  colors: {
    background: '#f2f2f2',
    text: '#121214',
    primary: '#455af7'
  }
}

export default theme
